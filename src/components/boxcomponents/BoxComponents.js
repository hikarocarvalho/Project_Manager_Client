import React from "react";
import "./BoxComponents.css";
const BoxComponents = ({children}) => (
    <section className="boxcomponents">
        {children}
    </section>
)
export default BoxComponents;